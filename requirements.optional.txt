# Optional archive quality / tagging integrations.
# Install only the tools you want to test:
#   pip install -r requirements.optional.txt

# Image duplicate / near-duplicate candidate detection.
# License: Apache-2.0
imagededup>=0.3

# Image quality audit: blurry, dark, low-information, odd-size, duplicates, etc.
# License note: CleanVision is AGPL-3.0. Keep it optional and review license fit before distribution.
cleanvision>=0.3

# Recognize Anything (RAM/RAM++) is installed from GitHub when needed.
# License: Apache-2.0
# Recommended POC install:
#   pip install git+https://github.com/xinyu1205/recognize-anything.git

# SigLIP / SigLIP 2 retrieval benchmark.
# License: model-dependent; google/siglip2-* checkpoints are Apache-2.0 on Hugging Face.
transformers>=4.49
accelerate>=0.30
sentencepiece>=0.2
safetensors>=0.4
